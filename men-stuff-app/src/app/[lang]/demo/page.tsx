import { LayoutExample } from '@/components/layout/LayoutExample'

/**
 * Demo page: shadcn layout example (dark navbar, product cards, accent buttons).
 * Visit /vi/demo or /en/demo to see the luxury men style.
 */
export default function DemoPage() {
  return <LayoutExample />
}
